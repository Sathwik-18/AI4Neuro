# Unified Neuro Platform - API layer dependencies.
# Kept intentionally small: the FastAPI image should not carry ML/torch weight.
# Modality-specific ML deps live in eeg.txt / mri.txt so worker images can
# install only what they need (see UNIFIED_NEURO_PLATFORM_ARCHITECTURE_PLAN.md 3.3).

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.13.4
pydantic-settings==2.7.1
python-multipart==0.0.20
httpx==0.28.1

# Supabase (modern key support: sb_publishable on frontend, sb_secret on backend)
supabase==2.31.0

# Auth: verify Supabase-issued JWTs.
# The crypto extra is required for newer Supabase asymmetric signing keys
# (for example ES256) discovered through JWKS.
PyJWT[crypto]==2.13.0

# Shared utilities
python-dotenv==1.0.1
cachetools==7.1.6

# Rate limiting
slowapi==0.1.9
