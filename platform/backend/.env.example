# Unified Neuro Platform - backend environment.
# Copy to .env and fill in. .env is gitignored; never commit real secrets.

# ---- Application ----
APP_ENV=development
API_HOST=0.0.0.0
API_PORT=8000
CORS_ORIGINS=http://localhost:3000

# ---- Supabase ----
# The backend uses a server-only elevated key. Prefer the newer Supabase
# `sb_secret_...` key. Legacy `service_role` JWT keys still work but should only
# be used as a fallback.
#
# SUPABASE_JWT_SECRET is optional for projects still issuing legacy HS256 user
# tokens. Newer Supabase projects use asymmetric JWT signing keys; the backend
# verifies those automatically through the project's JWKS endpoint.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_JWT_SECRET=

# ---- Storage buckets ----
RAW_FILES_BUCKET=raw-files
REPORT_ASSETS_BUCKET=report-assets
REPORTS_BUCKET=reports
VIEWER_SLICES_BUCKET=viewer-slices

# ---- Background jobs ----
JOB_BACKEND=local
LOCAL_JOB_MAX_WORKERS=2

# ---- Uploads ----
LOCAL_TMP_DIR=/tmp/neuro-platform
MAX_UPLOAD_MB=512

# ---- Auth ----
# Keep true for local dev before full auth is wired; MUST be false in production.
AUTH_DEV_BYPASS=true

# ---- Pipeline toggles ----
# MRI: mock model on hosts without ConViT weights / CAT12 / MATLAB Runtime (e.g. Linux).
USE_MOCK_MODEL=true
USE_CAT12_PREPROCESSING=false

# ---- EEG pipeline ----
# Defaults already point at ../../Alzheimer-Detection/backend/SIDDHI (relative to
# this repo layout), so these are only needed if you move/containerize the backend.
# EEG_SIDDHI_DIR=
# EEG_CHECKPOINT_ROOT=
# EEG_REFERENCE_DIR=
# EEG_USE_GPU=false

# ---- MRI pipeline ----
# Only needed for REAL MRI inference (USE_MOCK_MODEL=false). Requires a ConViT
# checkpoint, and CAT12 + MATLAB Runtime for USE_CAT12_PREPROCESSING=true
# (Windows/VM only — not portable to this sandbox/Linux).
# CONVIT_CHECKPOINT_PATH=
# CAT12_ROOT=
# CAT12_EXE=
# MCR_ROOT=
# MRI_USE_GPU=false

# ---- Real model paths after artifact sync ----
# If models were synced with platform/scripts/sync_models_from_object_storage.sh,
# use paths like these when USE_MOCK_MODEL=false:
# EEG_CHECKPOINT_ROOT=/absolute/path/to/AI4Neuro/platform/backend/models/eeg/checkpoints
# EEG_REFERENCE_DIR=/absolute/path/to/AI4Neuro/platform/backend/models/eeg/reference
# CONVIT_CHECKPOINT_PATH=/absolute/path/to/AI4Neuro/platform/backend/models/mri/ConVit_checkpoint.pth
